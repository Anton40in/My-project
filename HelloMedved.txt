import string
 
body = []
with open("HelloMedved.txt") as thisfile:
    i = 0
    body = thisfile.readlines()
 
with open("HelloMedved.txt", 'w') as new_file:
    for line in body:
        list = line.rstrip("\n").split(" ")
        i=i+1
        print list
        for word in list:
            if word == "Hello Medved":
                list.remove("Hello Medved")
        myString = ' '.join(list)
        new_file.write("{}\n".format(myString))
